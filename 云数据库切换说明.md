# 云数据库切换说明

当前后端已经从“Supabase 专用变量名”调整为“通用云 PostgreSQL 接法”。
也就是说，业务代码不用改，只要换数据库连接参数，就能切到新的云数据库。

## 推荐方案

推荐优先使用阿里云 RDS PostgreSQL。

原因：

- 国内网络更稳，手机真机演示时更省心。
- 现在后端本身就是按 PostgreSQL 写的，切过去不用改表结构和业务代码。
- 后面如果你要把 Spring Boot 后端也部署到阿里云 ECS，会比较顺手。

阿里云官方文档：

- [创建 PostgreSQL 实例](https://help.aliyun.com/zh/rds/apsaradb-rds-for-postgresql/create-an-apsaradb-rds-for-postgresql-instance)
- [连接 PostgreSQL 实例](https://help.aliyun.com/zh/rds/apsaradb-rds-for-postgresql/connect-to-an-apsaradb-rds-for-postgresql-instance)

## 现在读取的环境变量

后端现在读取的是下面这组环境变量：

```env
DATABASE_URL
DATABASE_USERNAME
DATABASE_PASSWORD
```

示例：

```env
DATABASE_URL=jdbc:postgresql://pgm-xxxxxxxxxxxx.pg.rds.aliyuncs.com:5432/community_micro_logistics?sslmode=require
DATABASE_USERNAME=community_admin
DATABASE_PASSWORD=你的数据库密码
SPRING_PROFILES_ACTIVE=prod
```

模板文件：

- [backend/.env.cloud.example](C:/Users/eddy2/Documents/软件工程-组一辈子乐队/编码/backend/.env.cloud.example)

## 第一次初始化云数据库

第一次要先建表并导入演示数据。

当前 Spring Boot 运行时的唯一推荐结构是：

- Supabase：执行 `db/migrations/20260901_backend_runtime_schema.sql`。
- 阿里云 RDS PostgreSQL：执行 `backend/src/main/resources/schema.sql`。

不要把 `db/migrations/20260525_phase1_supabase_schema.sql` 与当前结构混合执行；它是早期 Supabase Auth + `profiles` 方案，表名和字段类型与当前后端实体不同。

进入 [backend/scripts/seed-cloud-db.ps1](C:/Users/eddy2/Documents/软件工程-组一辈子乐队/编码/backend/scripts/seed-cloud-db.ps1) 所在目录后执行：

```powershell
$env:DATABASE_URL="jdbc:postgresql://你的实例地址:5432/community_micro_logistics?sslmode=require"
$env:DATABASE_USERNAME="community_admin"
$env:DATABASE_PASSWORD="你的密码"
$env:APP_JWT_SECRET="至少32位的随机字符串"
$env:APP_CORS_ALLOWED_ORIGIN_PATTERNS="https://你的前端域名"
.\seed-cloud-db.ps1
```

这个脚本会：

1. 连接新的云 PostgreSQL。
2. 执行 `schema.sql` 建表。
3. 执行 `data.sql` 导入测试用户、任务、拼单数据。
4. 自动退出，不会一直挂着。

## 正式启动后端

初始化完成后，正常启动时不要再带 `seed`。

```powershell
$env:SPRING_PROFILES_ACTIVE="prod"
$env:DATABASE_URL="jdbc:postgresql://你的实例地址:5432/community_micro_logistics?sslmode=require"
$env:DATABASE_USERNAME="community_admin"
$env:DATABASE_PASSWORD="你的密码"
$env:APP_JWT_SECRET="至少32位的随机字符串"
$env:APP_CORS_ALLOWED_ORIGIN_PATTERNS="https://你的前端域名"
.\gradlew.bat bootRun
```

## 现阶段说明

- 前端调用的还是后端 API，不会再自己硬塞本地假数据。
- 这次切换的是数据库底座，不是后端公网部署本身。
- 如果你下一步要做到手机完全脱离电脑独立运行，还需要把 Spring Boot 后端也部署到公网服务器，再把 Flutter 的 API 地址改成公网域名。
