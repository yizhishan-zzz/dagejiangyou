# 打个酱油 · 管理员端使用说明（对接真实后端）

管理员端是单文件 <code>admin.html</code>，**直接调用现有 Spring Boot 后端的真实数据库**（不再是本地假数据）。

## 一、三个模块

| 模块 | 后端接口 | 说明 |
|---|---|---|
| 数据总览 | GET /admin/overview | 用户数、任务数、**今日访问 / 总访问次数** |
| 账号管理 | GET /admin/users + PATCH /admin/users/{id}/status | 搜索/筛选、停用/恢复账号 |
| 任务管理 | GET /admin/tasks | 查看/筛选任务（发布人/接单人映射昵称） |

## 二、登录

- 管理员账号：<code>13800000099</code> / <code>demo123456</code>（后端 system_role=ADMIN）
- 登录走 <code>POST /users/auth/password-login</code> 拿 JWT，之后接口带 <code>Authorization: Bearer</code>

## 三、如何启动

1. 启动后端（dev 用 H2 内存库自动灌数据）：

       cd backend
       set JAVA_HOME=C:\javajdk21
       .\gradlew.bat bootRun --args="--spring.profiles.active=dev"

2. 用 HTTP 打开（<b>不能 file:// 双击</b>，后端 CORS 只放行 4100 等源）：

       python -m http.server 4100 --bind 127.0.0.1

   访问 <code>http://127.0.0.1:4100/admin.html</code>

3. 登录页可改「后端地址」，默认 <code>http://127.0.0.1:8080/api/v1</code>。

## 四、后端改动（均为「新增功能」，未改现有业务逻辑）

- 新增 <code>visit_logs</code> 表（schema.sql）
- 新增 <code>VisitLogEntity.kt</code>、<code>VisitLogRepository.kt</code>
- <code>AuthService</code> 登录成功时记录一条访问日志
- <code>AdminOverviewResponse</code> 增加 <code>totalVisits</code>、<code>todayVisits</code>
- <code>AdminService.overview()</code> 返回访问统计

## 五、验证情况（已通过）

- Kotlin 编译 <code>BUILD SUCCESSFUL</code>
- 后端 <code>/actuator/health</code> = UP（dev/H2）
- 管理员登录成功（token 长度 236）
- <code>GET /admin/overview</code> 返回 totalVisits/todayVisits；登录 2 次后 totalVisits 1→2（统计正确累加）
- 账号管理：4 个用户、停用/恢复成功；任务管理：2 个任务
- CORS 预检 200（放行 4100 + authorization）

## 六、关于「启动演示.bat」为何起不来

<code>启动演示.bat</code> → <code>start_demo.ps1</code> → <code>start_web_demo.ps1</code> 是一套「后端 + Flutter web 前端」全栈演示启动器。它强制要求存在 <code>frontend/build/web/index.html</code>（Flutter web 构建产物）；本机未装 Flutter，无法执行 <code>flutter build web</code>，因此该 .bat 会在启动任何服务前就报 <code>Missing built web app</code> 并退出。管理员端也不在这套 .bat 里，需按上面第三节命令手动启动。
