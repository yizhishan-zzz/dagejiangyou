# 打个酱油 · 启动与管理员端使用指南

本指南说明如何启动项目，以及如何使用**管理员端**（本地单文件 <code>admin.html</code>）。

## 一、项目概览

项目包含三部分：

| 端 | 技术 | 说明 |
|---|---|---|
| 双用户端 | Flutter | 发布者（CREATOR）/ 跑者（RUNNER）双模式 App |
| 后端 | Kotlin + Spring Boot | REST API，本地 dev 用 H2 内存库 |
| 管理员端 | 单文件 HTML | 前后端不分离、无服务器、数据存浏览器 localStorage |

管理员端与后端 <code>/api/v1/admin/**</code> 的数据模型保持一致（用户 / 任务 / 纠纷 / 状态枚举），但**不依赖后端**，数据完全存在本地浏览器。

## 二、环境要求

- 后端：JDK 21（本机路径 <code>C:\javajdk21</code>）
- 管理员端：任意现代浏览器即可（Chrome / Edge）
- 前端（可选）：Flutter SDK
- 生产数据库：PostgreSQL（本地演示**不需要**，dev 用 H2 内存库）

## 三、启动项目

### 3.1 启动后端（dev，H2 内存库，自动灌入演示数据）

在项目根目录执行：

    cd backend
    set JAVA_HOME=C:javajdk21
    .gradlew.bat bootRun --args="--spring.profiles.active=dev"

或 PowerShell 写法：

    $env:JAVA_HOME='C:javajdk21'
    cd backend
    .gradlew.bat bootRun --args='--spring.profiles.active=dev'

启动后：

- 端口：<code>8080</code>
- 健康检查：<code>http://127.0.0.1:8080/actuator/health</code>（返回 status=UP）
- dev 配置会**自动灌入演示数据**（DevDataSeeder），无需手动建表

演示账号（密码均为 <code>demo123456</code>）：

| 手机号 | 角色 |
|---|---|
| 13800000001 | 发布者（林晓雪） |
| 13800000002 | 跑者（周子衡） |
| 13800000003 | 邻居（陈阿姨） |
| 13800000099 | 后端管理员（system_role=ADMIN，供 /api/v1/admin/** 接口使用） |

> 注意：首次启动 Gradle 会下载依赖，可能需要几分钟。

### 3.2 启动管理员端（本地单文件，无需后端）

**方式一（最简单）**：直接双击项目根目录的 <code>admin.html</code>，用浏览器打开。

**方式二（静态服务器，得到固定 URL）**，在项目根目录执行：

    python -m http.server 4100 --bind 127.0.0.1

然后浏览器访问：

    http://127.0.0.1:4100/admin.html

### 3.3 前端（可选）

前端是 Flutter 项目，需要 Flutter SDK：

    cd frontend
    flutter build web --release

构建产物在 <code>frontend\build\web</code>，再用任意静态服务器托管即可。
> 当前这台机器未安装 Flutter，因此 web 前端暂时无法在本机构建；后端与管理员端不受影响。

## 四、管理员端使用（重点）

### 4.1 登录

- 默认账号：<code>admin</code>
- 默认密码：<code>admin123</code>

> 这是**本地管理员**账号，仅保存在当前浏览器，与后端管理员账号 <code>13800000099 / demo123456</code> 是两个独立的东西。登录后可在右上角「修改密码」。

### 4.2 数据总览

打开即进入看板，展示：

- 总用户数 / 正常账号 / 已停用账号 / 总任务数
- 待接单 / 进行中 / 已完成 / 待处理纠纷
- 「待处理纠纷」列表与「最近任务」列表，可直接点按钮跳转到对应模块

### 4.3 用户管理

1. 左侧点「用户管理」。
2. 顶部输入框搜索昵称或手机号；下拉框按状态筛选（正常 / 已停用 / 已注销）。
3. 每行右侧操作按钮：
   - **停用**：将该用户置为「已停用」。
   - **恢复**：把已停用用户恢复为「正常」。
   - **调信用分**：弹出窗口输入 0~100 的分值。
   - **详情**：查看完整资料。
4. 管理员角色账号（system_role=ADMIN）显示「不可操作」，与后端规则一致。

### 4.4 任务管理

1. 左侧点「任务管理」。
2. 搜索标题、按状态筛选（待接单 / 已接单 / 已取件 / 已到达 / 已完成 / 已取消）。
3. **改状态**：弹出下拉框选择新状态并保存。
4. **详情**：查看任务完整信息（发布人、接单人、小费、描述等）。

### 4.5 纠纷处理

1. 左侧点「纠纷处理」。
2. 按状态筛选（待处理 / 审核中 / 已解决 / 已驳回）。
3. 对「待处理 / 审核中」的纠纷：
   - **解决**：填写处理结论，状态变为「已解决」。
   - **驳回**：填写驳回理由（可选），状态变为「已驳回」。
4. **详情**：查看纠纷完整信息与处理结论。

### 4.6 数据管理

- **导出 JSON 备份**：把全部数据下载为 .json 文件。
- **导入 JSON 恢复**：选择之前导出的 .json，覆盖当前本地数据。
- **重置为演示数据**：清空并恢复内置演示数据（管理员密码也重置为 admin / admin123）。

## 五、数据与备份

- 所有数据保存在浏览器 <code>localStorage</code>，键名 <code>dsj_admin_db_v1</code>。
- **清空浏览器缓存 / 换浏览器 / 换电脑会丢失数据**，请定期「导出 JSON 备份」。
- 换机器恢复：打开 <code>admin.html</code> → 登录 → 数据管理 → 导入之前导出的 JSON。

## 六、常见问题

**Q：忘记本地管理员密码怎么办？**
A：在「数据管理 → 重置为演示数据」会连同密码一起重置为 admin / admin123；或在浏览器开发者工具里删除 <code>localStorage</code> 的 <code>dsj_admin_db_v1</code> 后刷新。

**Q：后端启动很慢？**
A：首次构建要下载 Gradle 依赖，属正常现象；之后再启动会快很多。

**Q：8080 端口被占用？**
A：关闭占用进程，或改用其它端口启动：<code>--args='--spring.profiles.active=dev --server.port=8081'</code>。

**Q：管理员端要不要连后端？**
A：不需要。它是纯本地、前后端不分离的单文件，双击即可用。
